import { metrics } from "@/lib/business";
import type { Lead } from "@/lib/types";
import { Title } from "./dashboard-view";
export function AnalyticsView({leads}:{leads:Lead[]}){const m=metrics(leads);return <><div className="stats analytics"><Stat label="Sudah dihubungi" value={m.contacted} note="pesan ditandai terkirim"/><Stat label="Tingkat balasan" value={m.responseRate} note="berdasarkan catatan tim" percent/><Stat label="Menjadi klien" value={m.conversionRate} note="dari seluruh calon klien" percent/><Stat label="Berhasil jadi klien" value={m.deal} note="hasil akhir yang dicatat"/></div><section className="panel"><Title title="Calon klien menurut jenis usaha"/><div className="category-bars">{Object.entries(leads.reduce<Record<string,number>>((all,lead)=>(all[lead.category]=(all[lead.category]||0)+1,all),{})).sort((a,b)=>b[1]-a[1]).map(([category,count])=><div key={category}><span>{category}</span><i><b style={{width:`${count/Math.max(1,leads.length)*100}%`}}/></i><strong>{count}</strong></div>)}</div></section></>}
function Stat({label,value,note,percent}:{label:string;value:number;note:string;percent?:boolean}){return <article className="stat"><p>{label}</p><b>{value}{percent&&"%"}</b><small>{note}</small></article>}
